PORT=5000
// Backend configuration
// PORT is the port the backend will run on
PORT=5000

# Firebase Admin (server) configuration - REQUIRED for server-side auth
# Provide one of the following (do NOT commit secret key to source control):
# 1) FIREBASE_SERVICE_ACCOUNT: base64-encoded service account JSON (recommended for CI/env)
#    Example (PowerShell):
#      $json = Get-Content .\serviceAccountKey.json -Raw
#      $b64 = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($json))
#      # then set FIREBASE_SERVICE_ACCOUNT to the base64 string
# 2) FIREBASE_SERVICE_ACCOUNT_PATH: absolute path to the service account JSON file on disk

FIREBASE_SERVICE_ACCOUNT=
# Set this to the absolute path of your Firebase service account JSON file.
# I placed a recommended location inside the backend folder; put your file there or change this path.
FIREBASE_SERVICE_ACCOUNT_PATH=C:\Users\yarla\OneDrive\Desktop\DocEase\backend\serviceAccountKey.json
FIREBASE_PROJECT_ID=docease-62051

# Note: This backend uses Firebase Admin to verify ID tokens for authentication.
